queue_rules:
  - name: vm3-native-stack-member-policy
    queue_conditions:
      - -draft
    merge_conditions:
      - label = vm3-native-stack-merge-ok-260918
    merge_method: merge
    batch_size: 1
merge_queue:
  mode: serial
  max_parallel_checks: 1
