queue_rules:
  - name: vm3-native-stack-order
    queue_conditions:
      - -draft
    merge_conditions:
      - check-success = vm3-native-stack-never-260918
    merge_method: merge
    batch_size: 1
merge_queue:
  mode: serial
  max_parallel_checks: 1
