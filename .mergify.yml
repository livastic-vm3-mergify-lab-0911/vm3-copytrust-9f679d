queue_rules:
  - name: vm3-stack-member-policy
    queue_conditions:
      - base = main
      - label = vm3-stack-admit-260918
    merge_conditions:
      - label = vm3-stack-merge-ok-260918
    merge_method: merge
    batch_size: 3
merge_queue:
  mode: serial
  max_parallel_checks: 1
