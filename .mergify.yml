pull_request_rules:
  - name: VM3 trusted reviewer login policy
    conditions:
      - "title ~= ^VM3 REVIEWER (TRUSTED CONTROL|USERNAME REUSE TREATMENT) 260920$"
      - approved-reviews-by = livastic-vm3-primary-20260911
      - check-success = vm3-reviewer-native-gate
    actions:
      merge:
        method: merge
