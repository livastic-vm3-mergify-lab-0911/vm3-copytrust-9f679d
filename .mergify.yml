queue_rules:
  - name: phase-probe
    routing_conditions:
      - title = VM3 MIGRATION PHASE PROBE 260918
    conditions:
      - check-success = vm3-never-pass-phase-probe-260918
    merge_method: merge
merge_queue:
  mode: serial
  max_parallel_checks: 1
