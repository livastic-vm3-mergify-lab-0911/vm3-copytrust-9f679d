commands_restrictions:
  queue:
    conditions:
      - sender = livastic-vm3-primary-20260911
  dequeue:
    conditions:
      - sender = livastic-vm3-primary-20260911
queue_rules:
  - name: vm3-stack-cascade
    queue_conditions: []
    merge_conditions:
      - check-success = vm3-stack-cascade-gate
    merge_method: merge
    batch_size: 2
merge_queue:
  mode: serial
  max_parallel_checks: 1
