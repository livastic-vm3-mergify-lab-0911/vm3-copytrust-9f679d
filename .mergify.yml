pull_request_rules:
  - name: vm3 request-review current-team freshness
    conditions:
      - base = main
    actions:
      request_reviews:
        users_from_teams:
          - required-reviewers
