commands_restrictions:
  queue:
    conditions:
      - sender = livastic-vm3-secondary-20260911
  dequeue:
    conditions:
      - sender = livastic-vm3-primary-20260911
queue_rules:
  - name: vm3-fork-stack-provenance
    queue_conditions: []
    merge_conditions:
      - check-success = vm3-fork-stack-never
    merge_method: merge
    batch_size: 2
merge_queue:
  mode: serial
  max_parallel_checks: 1
