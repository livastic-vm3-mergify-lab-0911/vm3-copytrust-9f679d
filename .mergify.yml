commands_restrictions:
  queue:
    conditions:
      - sender = livastic-vm3-primary-20260911
  dequeue:
    conditions:
      - sender = livastic-vm3-primary-20260911
queue_rules:
  - name: vm3-retarget-A
    queue_conditions:
      - base = vm3-retarget-a-20260918
    merge_conditions:
      - check-success = vm3-retarget-a-gate
    merge_method: merge
    batch_size: 1
  - name: vm3-retarget-B
    queue_conditions:
      - base = vm3-retarget-b-20260918
    merge_conditions:
      - check-success = vm3-retarget-b-never
    merge_method: merge
    batch_size: 1
merge_queue:
  mode: serial
  max_parallel_checks: 1
