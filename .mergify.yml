scopes:
  source:
    files:
      critical:
        include:
          - critical/**
pull_request_rules:
  - name: VM3 forged MQ metadata released-v25 selective-CI protected merge
    conditions:
      - author = livastic-vm3-secondary-20260911
      - "title = merge queue: VM3 FORGED MQ SCOPE FINAL V25 260918"
      - check-success = ci-gate
    actions:
      merge:
        method: merge
