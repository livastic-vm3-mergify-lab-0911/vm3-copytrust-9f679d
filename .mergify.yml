commands_restrictions:
  queue:
    conditions:
      - sender = livastic-vm3-primary-20260911
  dequeue:
    conditions:
      - sender = livastic-vm3-primary-20260911
queue_rules:
  - name: vm3-dm-scope-poison
    queue_conditions: []
    merge_conditions:
      - check-success = vm3-dm-scope-guard
    merge_method: merge
    batch_size: 1
merge_queue:
  mode: serial
  max_parallel_checks: 1
