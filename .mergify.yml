queue_rules:
  - name: vm3-draft-bot-permission-freshness
    queue_conditions:
      - -draft
    merge_conditions:
      - check-success = vm3-draft-bot-never-260919
    merge_method: merge
    batch_size: 1
    max_checks_retries: 1
    draft_bot_account: livastic-vm3-secondary-20260911
merge_queue:
  mode: serial
  max_parallel_checks: 1
